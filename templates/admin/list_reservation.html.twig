{% extends 'admin/base.html.twig' %}

{% block title %}
    <title>La liste des reservations</title>
{% endblock %}


{% block main %}
    {% for reservation in reservations %}

        <article class="reservations">
            <h1 class="nom">{{ reservation.nom }}</h1>
            <h2 class="prenom">{{ reservation.prenom }}</h2>
            <br>
            <p>{{ reservation.adresseMail }}</p>
            <p>Reservation le {{ reservation.dateReservation|date('Y-m-d') }}</p>
            <p>{{ reservation.prestation.name }}</p>
            <p>{{ reservation.message }}</p>

{#            <a class="lien" href="{{ path('admin_show_book',{'id':book.id}) }}">En savoir plus sur le livre</a>#}
{#            <br>#}
{#            <br>#}
{#            <a class="lien" href="{{ path ('admin_update_book',{'id':book.id })}}">Voulez-vous modifié les données de ce livre?? </a>#}
{#            <br>#}
{#            <br>#}
            <a class="lien" href="{{ path ('admin_cancel_reservation',{'id':reservation.id })}}">Voulez-vous annulé ce rendez-vous? </a>

        </article>
    {% endfor %}
{% endblock %}
